@model List<BigBank.Models.FDAccount>

@{
    ViewBag.Title = "FD Accounts";
}

<div class="container py-4">
    <h2>FD Accounts</h2>
    <div class="card">
        <div class="card-body">
            <table class="table table-sm">
                <thead><tr><th>Sr. No.</th><th>FD Account ID</th><th>Customer ID</th><th>Amount</th><th>Start</th><th>End</th></tr></thead>
                <tbody>
                    @for (int i=0;i<Model.Count;i++) {
                        var a = Model[i];
                        <tr>
                            <td>@(i+1)</td>
                            <td>@a.FDAccountID</td>
                            <td>@a.CustomerID</td>
                            <td>@("Rs " + a.Amount.ToString("N2"))</td>
                            <td>@a.StartDate.ToShortDateString()</td>
                            <td>@a.EndDate.ToShortDateString()</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>